# Python Environment

This project uses uv for Python dependency management.

## Running Python Commands

**Important for Windows**: Do NOT try to activate the virtual environment with `.venv\Scripts\activate`. Instead, prefix all Python commands with `uv run`:
```bash
# Run scripts
uv run python script.py
```
